SQL practice queries covering:

- Data retrieval using SELECT and WHERE  
- Sorting and filtering with ORDER BY  
- Aggregations using GROUP BY and HAVING  
- Aggregate functions (COUNT, SUM, AVG, MIN, MAX)  
- Table joins (INNER, LEFT, RIGHT, SELF)  
- Subqueries (nested and correlated)  
- String functions for text manipulation  
- Date and time manipulation functions  
- Window functions for analytical queries  
- Views for reusable query logic  
- Common Table Expressions (CTEs)  
- Stored procedures and triggers  
